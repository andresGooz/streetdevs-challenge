# para desplegar, ejecutar por terminal:
docker compose up -d

# entrar a la base de datos
sudo docker exec -it mysqlService mysql -uuseruser -p
Ingresa la contraseña: admin (como sale en el docker compose)
USE nodejs;
SHOW TABLES;
